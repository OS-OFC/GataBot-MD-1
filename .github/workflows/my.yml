name: Run nodejs index.js
on: [push]
jobs:
 MoroccoAI Server ✅ :
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - run: npm i && node .
